[package]
name = "soro-fuzz-invariants"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Reusable library of Invariant implementations for soro-fuzz"

[dependencies]
soro-fuzz-core = { version = "0.1.0", path = "../core" }
soroban-sdk = { version = "27.0.6", features = ["testutils"] }
