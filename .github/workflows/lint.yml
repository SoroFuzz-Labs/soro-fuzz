name: lint

on:
  push:
    branches: [master]
  pull_request:

jobs:
  fmt:
    name: rustfmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all --check

  clippy:
    name: clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      # fuzz/ is a separate workspace root (nightly + libfuzzer-sys) and is
      # excluded here, matching ci.yml; --workspace covers only the engine.
      - run: cargo clippy --workspace --all-targets -- -D warnings
